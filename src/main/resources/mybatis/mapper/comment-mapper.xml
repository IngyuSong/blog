<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blog.mapper.CommentMapper">
    <select id="findByPage" resultType="com.example.blog.vo.Comment">
        SELECT *
        FROM comment
        WHERE post_idx = #{post_idx}
        ORDER BY idx DESC
        LIMIT #{getCount} OFFSET #{pageNum}
    </select>
    <insert id="save">
        INSERT INTO comment(content, username, createdAt, post_idx)
        VALUES (#{comment.title}, #{comment.content}, #{comment.username}, now(), #{post_idx})
    </insert>
    <update id="update">
        UPDATE comment
        SET title = #{comment.username}, content=#{comment.content}
        WHERE idx = #{comment.idx}
    </update>
    <delete id="delete">
        DELETE
        FROM comment
        WHERE idx = #{idx}
    </delete>
    <select id="count" resultType="Integer">
        SELECT COUNT(*)
        FROM comment
        WHERE post_idx = #{post_idx}
    </select>
</mapper>